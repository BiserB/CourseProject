@model ICollection<PollResultModel>
@{
    ViewData["Title"] = "PollResults";
}

@{ 
    foreach (var poll in Model)
    {
        <p>@poll.PollQuestion </p>
        <hr class="style3"/>
        foreach (var answer in poll.PollAnswers)
        {
            <p>@answer.Content - @answer.Votes</p>
        }
    }
}
